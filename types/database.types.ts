/**
 * Placeholder for generated Supabase database types.
 *
 * After the real schema is created, generate this file with the Supabase CLI:
 *
 * npx supabase gen types typescript \
 *   --project-id YOUR_PROJECT_ID \
 *   --schema public > types/database.types.ts
 *
 * The initialization backend deliberately does not depend on this placeholder
 * so the project can be started before domain tables exist.
 */
export {};
